@model IEnumerable<POPS.Models.ITEM>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ITDESC)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ITRATE)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ITDESC)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ITRATE)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ITCODE }) |
            @Html.ActionLink("Details", "Details", new { id=item.ITCODE }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ITCODE })
        </td>
    </tr>
}

</table>
